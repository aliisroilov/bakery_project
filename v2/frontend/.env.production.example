# V2 frontend production build config
# Copy to `.env.production` before `npm run build`.

# The API base URL the React app will talk to.
# For a same-origin Nginx setup this is just `/api/v1`.
VITE_API_BASE=/api/v1
